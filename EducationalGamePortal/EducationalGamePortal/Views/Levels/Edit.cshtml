@model EducationalGamePortal.Models.Level

@{
    ViewData["Title"] = "Редагувати рівень";
}

<h1>Редагувати рівень</h1>

<div class="row">
    <div class="col-md-6">
        <form asp-action="Edit">
            <div asp-validation-summary="All" class="text-danger"></div>

            <input type="hidden" asp-for="Id" />

            <div class="mb-3">
                <label asp-for="GameId" class="form-label">Гра</label>
                <select asp-for="GameId" asp-items="@ViewBag.Games" class="form-control"></select>
            </div>

            <div class="mb-3">
                <label asp-for="LevelNumber" class="form-label">Номер рівня</label>
                <input asp-for="LevelNumber" class="form-control" type="number" min="1" />
            </div>

            <div class="mb-3">
                <label asp-for="Title" class="form-label">Назва рівня</label>
                <input asp-for="Title" class="form-control" />
            </div>

            <div class="mb-3">
                <label asp-for="Difficulty" class="form-label">Складність</label>
                <select asp-for="Difficulty" class="form-control">
                    <option value="Easy">Легкий</option>
                    <option value="Medium">Середній</option>
                    <option value="Hard">Складний</option>
                </select>
            </div>

            <div class="mb-3">
                <label asp-for="MaxScore" class="form-label">Максимальний бал</label>
                <input asp-for="MaxScore" class="form-control" type="number" min="1" />
            </div>

            <div class="mb-3">
                <button type="submit" class="btn btn-warning">Зберегти зміни</button>
                <a asp-action="Index" class="btn btn-secondary">Скасувати</a>
            </div>
        </form>
    </div>
</div>
